apiVersion: v1
kind: Secret
metadata:
  name: postgres-auth
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.postgres.user | quote }}
  POSTGRES_PASSWORD: {{ .Values.postgres.password | quote }}
  POSTGRES_DB: {{ .Values.postgres.database | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-auth
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
type: Opaque
stringData:
  MINIO_ROOT_USER: {{ .Values.minio.accessKey | quote }}
  MINIO_ROOT_PASSWORD: {{ .Values.minio.secretKey | quote }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: factory-api
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: factory-runner-controller
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: factory-api-role-{{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list", "create"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "create", "update", "patch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["get", "create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: factory-api-rolebinding-{{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: factory-api
    namespace: {{ include "factory.namespace" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: factory-api-role-{{ include "factory.namespace" . }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: factory-runner-controller-role-{{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["serviceaccounts"]
    verbs: ["get", "list", "create"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list", "create", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: factory-runner-controller-rolebinding-{{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: factory-runner-controller
    namespace: {{ include "factory.namespace" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: factory-runner-controller-role-{{ include "factory.namespace" . }}
{{- if .Values.selfDeploy.runnerAccess.enabled }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: factory-self-deploy-runner
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
rules:
  - apiGroups: ["apps"]
    resources: ["deployments", "deployments/status"]
    verbs: ["get", "list", "watch", "patch", "update"]
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: factory-self-deploy-runner
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.selfDeploy.runnerAccess.serviceAccountName | quote }}
    namespace: {{ .Values.selfDeploy.runnerAccess.projectNamespace | quote }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: factory-self-deploy-runner
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app.kubernetes.io/name: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
    app.kubernetes.io/name: postgres
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
        {{- include "factory.labels" . | nindent 8 }}
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.images.postgres.repository }}:{{ .Values.images.postgres.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 5432
          env:
            - name: PGDATA
              value: {{ .Values.postgres.pgData | quote }}
          envFrom:
            - secretRef:
                name: postgres-auth
          resources:
            {{- toYaml .Values.resources.postgres | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.postgres.storageClassName }}
        storageClassName: {{ .Values.postgres.storageClassName | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.postgres.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
spec:
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app.kubernetes.io/name: redis
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
    app.kubernetes.io/name: redis
spec:
  serviceName: redis
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
  template:
    metadata:
      labels:
        app.kubernetes.io/name: redis
        {{- include "factory.labels" . | nindent 8 }}
    spec:
      containers:
        - name: redis
          image: "{{ .Values.images.redis.repository }}:{{ .Values.images.redis.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 6379
          args: ["--appendonly", "yes"]
          resources:
            {{- toYaml .Values.resources.redis | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.redis.storageClassName }}
        storageClassName: {{ .Values.redis.storageClassName | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.redis.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
spec:
  ports:
    - port: 9000
      targetPort: 9000
      name: api
    - port: 9001
      targetPort: 9001
      name: console
  selector:
    app.kubernetes.io/name: minio
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: minio
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
    app.kubernetes.io/name: minio
spec:
  serviceName: minio
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: minio
  template:
    metadata:
      labels:
        app.kubernetes.io/name: minio
        {{- include "factory.labels" . | nindent 8 }}
    spec:
      containers:
        - name: minio
          image: "{{ .Values.images.minio.repository }}:{{ .Values.images.minio.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          args: ["server", "/data", "--console-address", ":9001"]
          ports:
            - containerPort: 9000
            - containerPort: 9001
          envFrom:
            - secretRef:
                name: minio-auth
          resources:
            {{- toYaml .Values.resources.minio | nindent 12 }}
          volumeMounts:
            - mountPath: /data
              name: data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.minio.storageClassName }}
        storageClassName: {{ .Values.minio.storageClassName | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.minio.storage }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: factory-api
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
    app.kubernetes.io/name: factory-api
spec:
  {{- if not .Values.hpa.api.enabled }}
  replicas: 1
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: factory-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: factory-api
        {{- include "factory.labels" . | nindent 8 }}
    spec:
      serviceAccountName: factory-api
      containers:
        - name: api
          image: "{{ .Values.images.api.repository }}:{{ .Values.images.api.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: ["sh", "-lc"]
          args: ["npm run prisma:migrate:deploy && node apps/factory-api/dist/apps/factory-api/src/main.js"]
          ports:
            - containerPort: {{ .Values.service.api.port }}
          env:
            - name: PORT
              value: {{ .Values.service.api.port | quote }}
            - name: FACTORY_VERSION
              value: {{ default "" .Values.global.factoryVersion | quote }}
            - name: REDIS_URL
              value: "redis://redis.{{ include "factory.namespace" . }}.svc.cluster.local:6379"
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres.{{ include "factory.namespace" . }}.svc.cluster.local:5432/{{ .Values.postgres.database }}"
            - name: RUNNER_IMAGE
              value: "{{ include "factory.runnerImageRef" . }}"
            - name: GITHUB_APP_SLUG
              value: {{ .Values.github.appSlug | quote }}
            - name: GITHUB_APP_ID
              value: {{ .Values.github.appId | quote }}
            - name: GITHUB_APP_PRIVATE_KEY
              value: {{ .Values.github.appPrivateKey | quote }}
            - name: GITHUB_WEBHOOK_SECRET
              value: {{ .Values.github.webhookSecret | quote }}
            - name: GITHUB_SYNC_ENABLED
              value: {{ .Values.github.syncEnabled | quote }}
            - name: GITHUB_RECONCILE_INTERVAL_MINUTES
              value: {{ .Values.github.reconcileIntervalMinutes | quote }}
            - name: SHELL_SESSION_TTL_SECONDS
              value: {{ .Values.shell.sessionTtlSeconds | quote }}
            - name: SHELL_POD_READY_TIMEOUT_SECONDS
              value: {{ .Values.shell.podReadyTimeoutSeconds | quote }}
            - name: TASK_TEMPLATE_SCHEDULER_ENABLED
              value: {{ .Values.taskTemplateScheduler.enabled | quote }}
            - name: TASK_TEMPLATE_SCHEDULER_INTERVAL_SECONDS
              value: {{ .Values.taskTemplateScheduler.intervalSeconds | quote }}
            - name: TASK_TEMPLATE_SCHEDULER_BATCH_SIZE
              value: {{ .Values.taskTemplateScheduler.batchSize | quote }}
            - name: FACTORY_AUTH_GOOGLE_CLIENT_ID
              value: {{ .Values.auth.googleClientId | quote }}
            - name: FACTORY_AUTH_GOOGLE_CLIENT_SECRET
              value: {{ .Values.auth.googleClientSecret | quote }}
            - name: FACTORY_AUTH_ALLOWED_DOMAIN
              value: {{ .Values.auth.allowedDomain | quote }}
            - name: FACTORY_AUTH_SESSION_SECRET
              value: {{ .Values.auth.sessionSecret | quote }}
            - name: MINIO_BUCKET
              value: {{ .Values.minio.bucket | quote }}
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-auth
                  key: MINIO_ROOT_USER
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-auth
                  key: MINIO_ROOT_PASSWORD
          resources:
            {{- toYaml .Values.resources.api | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: factory-api
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
  {{- with .Values.service.api.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app.kubernetes.io/name: factory-api
  ports:
    - name: http
      port: {{ .Values.service.api.port }}
      targetPort: {{ .Values.service.api.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: factory-web
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
    app.kubernetes.io/name: factory-web
spec:
  {{- if not .Values.hpa.web.enabled }}
  replicas: 1
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: factory-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: factory-web
        {{- include "factory.labels" . | nindent 8 }}
    spec:
      containers:
        - name: web
          image: "{{ .Values.images.web.repository }}:{{ .Values.images.web.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: ["node", "apps/factory-web/dist/server/main.js"]
          ports:
            - containerPort: {{ .Values.service.web.port }}
          env:
            - name: PORT
              value: {{ .Values.service.web.port | quote }}
            - name: FACTORY_VERSION
              value: {{ default "" .Values.global.factoryVersion | quote }}
            - name: API_BASE_URL
              value: {{ .Values.web.apiBaseUrl | quote }}
            - name: FACTORY_AUTH_GOOGLE_CLIENT_ID
              value: {{ .Values.auth.googleClientId | quote }}
            - name: FACTORY_AUTH_GOOGLE_CLIENT_SECRET
              value: {{ .Values.auth.googleClientSecret | quote }}
            - name: FACTORY_AUTH_ALLOWED_DOMAIN
              value: {{ .Values.auth.allowedDomain | quote }}
            - name: FACTORY_AUTH_SESSION_SECRET
              value: {{ .Values.auth.sessionSecret | quote }}
          resources:
            {{- toYaml .Values.resources.web | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: factory-web
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
  {{- with .Values.service.web.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app.kubernetes.io/name: factory-web
  ports:
    - name: http
      port: {{ .Values.service.web.port }}
      targetPort: {{ .Values.service.web.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: factory-runner-controller
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
    app.kubernetes.io/name: factory-runner-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: factory-runner-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: factory-runner-controller
        {{- include "factory.labels" . | nindent 8 }}
    spec:
      serviceAccountName: factory-runner-controller
      containers:
        - name: controller
          image: "{{ .Values.images.controller.repository }}:{{ .Values.images.controller.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: ["node", "apps/factory-runner-controller/dist/apps/factory-runner-controller/src/main.js"]
          env:
            - name: REDIS_URL
              value: "redis://redis.{{ include "factory.namespace" . }}.svc.cluster.local:6379"
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres.{{ include "factory.namespace" . }}.svc.cluster.local:5432/{{ .Values.postgres.database }}"
            - name: RUNNER_IMAGE
              value: "{{ include "factory.runnerImageRef" . }}"
            - name: FACTORY_API_BASE_URL
              value: "http://factory-api.{{ include "factory.namespace" . }}.svc.cluster.local:{{ .Values.service.api.port }}"
            - name: GITHUB_APP_ID
              value: {{ .Values.github.appId | quote }}
            - name: GITHUB_APP_PRIVATE_KEY
              value: {{ .Values.github.appPrivateKey | quote }}
            - name: GITHUB_TOKEN
              value: {{ .Values.github.token | quote }}
            - name: MINIO_ENDPOINT
              value: "http://minio.{{ include "factory.namespace" . }}.svc.cluster.local:9000"
            - name: MINIO_BUCKET
              value: {{ .Values.minio.bucket | quote }}
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-auth
                  key: MINIO_ROOT_USER
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-auth
                  key: MINIO_ROOT_PASSWORD
          resources:
            {{- toYaml .Values.resources.controller | nindent 12 }}
---
{{- if .Values.hpa.api.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: factory-api
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: factory-api
  minReplicas: {{ .Values.hpa.api.minReplicas }}
  maxReplicas: {{ .Values.hpa.api.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.api.cpuUtilization }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.api.memoryUtilization }}
---
{{- end }}
{{- if .Values.hpa.web.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: factory-web
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: factory-web
  minReplicas: {{ .Values.hpa.web.minReplicas }}
  maxReplicas: {{ .Values.hpa.web.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.web.cpuUtilization }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.web.memoryUtilization }}
---
{{- end }}
{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: factory-system
  namespace: {{ include "factory.namespace" . }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - {{- if .Values.ingress.host }}
      host: {{ .Values.ingress.host }}
      {{- end }}
      http:
        paths:
          - path: {{ .Values.ingress.apiPathPrefix | default "/api" }}
            pathType: Prefix
            backend:
              service:
                name: factory-api
                port:
                  number: {{ .Values.service.api.port }}
          - path: {{ .Values.ingress.webPathPrefix | default "/" }}
            pathType: Prefix
            backend:
              service:
                name: factory-web
                port:
                  number: {{ .Values.service.web.port }}
{{- end }}
